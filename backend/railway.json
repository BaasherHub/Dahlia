{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma migrate resolve --rolled-back 20260101_initial 2>/dev/null; npx prisma migrate deploy && node src/seed.js && node src/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
