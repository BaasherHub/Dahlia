# ─── API ──────────────────────────────────────────────────────────────────────
VITE_API_URL="https://your-backend.railway.app"

# ─── Style customization — change these to restyle the whole site! ─────────────
# (These are just defaults; the real values live in src/index.css :root block)
