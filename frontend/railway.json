{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --verbose && npx vite build 2>&1 && echo 'BUILD COMPLETE' && ls -la dist/ && ls -la dist/assets/"
  },
  "deploy": {
    "startCommand": "npx serve dist -s -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}